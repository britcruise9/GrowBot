# GrowBot agent instructions

Preserve the creature-first thesis and the existing mind/body contracts. Prefer one
complete vertical outcome over horizontal scaffolding, and keep physical safety below
model authority.

## Agent skills

### Issue tracker

Specs and implementation issues live in the writable `jordanhindo/GrowBot` fork while
`britcruise9/GrowBot` retains upstream authority. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the canonical five-role triage vocabulary. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repository. Read `CONTEXT.md` and relevant records under
`docs/adr/` before changing the product. See `docs/agents/domain.md`.
